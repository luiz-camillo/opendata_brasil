import styles from './DataExplorerPage.module.css'

/**
 * DataExplorerPage will provide filterable, virtualized tables for browsing
 * open datasets. Currently a placeholder pending data integration.
 */
function DataExplorerPage() {
  return (
    <div className={styles.placeholder}>
      <h1 className={styles.title}>Data Explorer</h1>
      <p className={styles.subtitle}>
        Exploração de conjuntos de dados abertos em breve.
      </p>
    </div>
  )
}

export default DataExplorerPage
